# ==============================================================
# AI Voice Assistant — Central Dependencies
# ==============================================================
# Install with:  pip install -r requirements.txt
# ==============================================================

# Backend
flask>=3.0.0
flask-cors>=5.0.0
python-dotenv>=1.0.1
requests>=2.31.0

# STT — local inference
# (only required when STT_USE_REMOTE=false in .env)
# If you use the Colab GPU server (STT_USE_REMOTE=true),
# you can skip faster-whisper/ctranslate2/torch on the local machine.
faster-whisper>=1.1.0
ctranslate2>=4.5.0
transformers>=4.50.0
sentencepiece>=0.2.0
torch>=2.0.0

# Audio
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0

# TTS
edge-tts>=6.0.0
pyttsx3>=2.90
pygame>=2.5.0

# Automation
playwright>=1.54.0
pyautogui>=0.9.54
pywin32>=308
psutil>=7.0.0
pyrogram>=2.0.0
tgcrypto>=1.2.0

# Database
sqlalchemy>=2.0.0

# Utilities
rich>=13.0.0
pydantic>=2.11.0
pytest>=8.0.0
jiwer>=3.0.0

# Context-Based Document Search (feature/context-document-search)
sentence-transformers>=2.7.0    # local semantic embeddings (all-MiniLM-L6-v2)
PyMuPDF>=1.24.0                 # PDF text extraction (import as fitz)
python-docx>=1.1.0              # DOCX text extraction
python-pptx>=0.6.23             # PPTX text extraction
openpyxl>=3.1.0                 # XLSX/XLS text extraction
rank-bm25>=0.2.2                # BM25 ranking (with TF fallback if unavailable)
faiss-cpu>=1.7.0                # FAISS vector store for faster search
watchdog>=4.0.0                 # Filesystem monitoring
pandas>=2.0.0                   # CSV parsing

# ==============================================================
# Colab-side dependencies (colab_stt_server.ipynb installs these)
# ==============================================================
# The notebook runs:
#   pip install faster-whisper ctranslate2 flask pyngrok
# You do NOT need to install these locally unless you want to
# run the STT server yourself.
